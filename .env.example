# Once you have a real domain, set this so SEO tags (OG, sitemap, robots) point to it.
NEXT_PUBLIC_SITE_URL=https://yourdomain.com

# Once MongoDB Atlas is set up (see comment in src/app/api/quote/route.ts)
# MONGODB_URI=
